## 🛠 Getting Started (readme.md part)
Clone the repository and navigate to the API type you want to explore:

```bash
git clone https://github.com/your-username/api-repo.git
cd api-repo/rest-api
npm install
node index.js

*****Future Extensions
Add authentication & authorization examples (JWT, OAuth2).

Integrate databases (PostgreSQL, MongoDB).

Provide Dockerized deployments for each API type.

Include testing frameworks (Jest, Mocha, Postman collections).
